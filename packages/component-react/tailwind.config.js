/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: "class",
    content: ["./**/*.{tsx,jsx}"],
    theme: {
        extend: {
            colors: {
                primary: '#297EFF'
            }
        },
    },
    plugins: [],
}
